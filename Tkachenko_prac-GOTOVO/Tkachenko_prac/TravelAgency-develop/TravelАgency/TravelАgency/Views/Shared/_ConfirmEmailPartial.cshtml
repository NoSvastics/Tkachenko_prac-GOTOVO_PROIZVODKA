<!-- _ConfirmEmailPartial.cshtml -->

<div class="confirm-email-container" style="display:none;">
    <div class="confirm-email-form">
        <div class="form_title">✉️ Подтверждение Email</div>

        <p>На указанную почту отправлено письмо с кодом подтверждения.</p>
        <p style="font-size: 13px; color: #80868b; margin-bottom: 20px;">
            Пожалуйста, введите код из письма ниже для завершения регистрации.
        </p>

        <label>
            <input type="text"
                   placeholder=" "
                   id="code_confirm"
                   maxlength="6"
                   style="text-align: center; font-size: 20px; letter-spacing: 10px; font-weight: bold;" />
            <span>Введите 6-значный код</span>
        </label>

        <div class="button_confirm_container">
            <button type="button" class="button_confirm send_confirm">
                ✓ Подтвердить
            </button>
            <button type="button" class="button_confirm red button_confirm_close">
                ✕ Отмена
            </button>
        </div>

        <p class="warning">
            ⚠️ При закрытии этого окна регистрация не будет завершена
        </p>
    </div>

    <div class="overlay"></div>
</div>

<style>
    /* ===== CONFIRM EMAIL CONTAINER ===== */
    .confirm-email-container {
        display: none;
        width: 100%;
        height: 100%;
        position: fixed;
        justify-content: center;
        align-items: center;
        background: rgba(0, 0, 0, 0.7);
        z-index: 1000;
        backdrop-filter: blur(5px);
        animation: fadeIn 0.3s ease;
    }

    @@keyframes fadeIn {
        from

    {
        opacity: 0;
    }

    to {
        opacity: 1;
    }

    }

    .confirm-email-form {
        margin: 100px auto;
        display: grid;
        padding: 40px;
        width: 480px;
        background: white;
        border-radius: 14px;
        box-shadow: 0 12px 32px rgba(0, 0, 0, 0.2);
        z-index: 901;
        animation: slideIn 0.5s ease;
    }

    @@keyframes slideIn {
        from

    {
        transform: translateY(-50px);
        opacity: 0;
    }

    to {
        transform: translateY(0);
        opacity: 1;
    }

    }

    .confirm-email-form input {
        outline: none;
        padding: 14px 16px;
        border: 2px solid #dadce0;
        border-radius: 10px;
        font-size: 15px;
        transition: all 0.3s ease;
        background: #f8f9fa;
        margin-bottom: 15px;
    }

        .confirm-email-form input:focus {
            border-color: #1a73e8;
            background: white;
            box-shadow: 0 0 0 4px rgba(26, 115, 232, 0.1);
        }

    .confirm-email-form .form_title {
        font-size: 1.8rem;
        margin-bottom: 20px;
        color: #202124;
        background: linear-gradient(135deg, #1a73e8, #34a853);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        font-weight: 800;
        text-align: center;
    }

    .confirm-email-form p {
        margin-bottom: 15px;
        color: #80868b;
        line-height: 1.6;
        font-size: 14px;
        font-weight: 500;
    }

    .confirm-email-form label {
        position: relative;
        display: block;
        margin-bottom: 25px;
    }

    .confirm-email-form span {
        cursor: text;
        position: absolute;
        top: 14px;
        left: 16px;
        color: #80868b;
        transition: all 0.3s ease-in-out;
        padding: 0 4px;
        background: #f8f9fa;
        font-size: 14px;
        font-weight: 500;
    }

    .confirm-email-form input:focus + span,
    .confirm-email-form input:not(:placeholder-shown) ~ span {
        top: -12px;
        left: 14px;
        font-size: 12px;
        color: #1a73e8;
        background: white;
        padding: 0 8px;
        font-weight: 700;
    }

    /* ===== CONFIRM BUTTONS ===== */
    .button_confirm_container {
        display: flex;
        gap: 15px;
        margin: 30px auto 0;
        justify-content: center;
    }

    .button_confirm {
        cursor: pointer;
        background: linear-gradient(135deg, #1a73e8, #1557b7);
        color: white;
        padding: 13px 30px;
        border-radius: 10px;
        border: none;
        font-weight: 700;
        transition: all 0.3s ease;
        box-shadow: 0 4px 15px rgba(26, 115, 232, 0.3);
        font-size: 14px;
        min-width: 130px;
    }

        .button_confirm:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(26, 115, 232, 0.4);
        }

        .button_confirm:active {
            transform: translateY(-1px);
        }

        .button_confirm.red {
            background: linear-gradient(135deg, #ea4335, #d93025);
            box-shadow: 0 4px 15px rgba(234, 67, 53, 0.3);
        }

            .button_confirm.red:hover {
                background: linear-gradient(135deg, #d93025, #c5221f);
                box-shadow: 0 8px 25px rgba(234, 67, 53, 0.4);
            }

    /* ===== WARNING ===== */
    .warning {
        padding: 14px 16px;
        color: #c5221f;
        background: #fce8e6;
        border-radius: 8px;
        font-size: 13px;
        text-align: center;
        border-left: 4px solid #ea4335;
        font-weight: 500;
        margin-top: 15px;
    }

    /* ===== OVERLAY ===== */
    .overlay {
        position: absolute;
        width: 100%;
        height: 100%;
        z-index: 900;
        cursor: pointer;
    }

    /* ===== RESPONSIVE ===== */
    @@media (max-width: 768px) {
        .confirm-email-form

    {
        width: 90%;
        max-width: 400px;
        margin: 100px auto;
        padding: 25px;
    }

    .confirm-email-form .form_title {
        font-size: 1.5rem;
    }

    .button_confirm {
        min-width: 100px;
        padding: 10px 20px;
        font-size: 13px;
    }

    }

    @@media (max-width: 600px) {
        .confirm-email-form

    {
        width: 95%;
        padding: 20px 15px;
        border-radius: 12px;
    }

    .button_confirm_container {
        flex-direction: column;
        gap: 10px;
    }

    .button_confirm {
        width: 100%;
        min-width: unset;
    }

    }
</style>